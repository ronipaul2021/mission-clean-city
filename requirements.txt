# Core Framework
Django==5.2.6

# Image Processing (for photo compression)
Pillow>=10.0.0

# HTTP Requests (for Fast2SMS API)
requests>=2.31.0

# Security: Aadhaar Encryption at rest
cryptography>=42.0.0

# Environment variable management (replaces hardcoded secrets in settings.py)
python-decouple>=3.8

# Production WSGI server (optional, for deployment)
gunicorn>=21.2.0

# AI-powered face detection & auto-crop for profile photos
opencv-python-headless

# PostgreSQL adapter (optional, for production DB switch)
# psycopg2-binary>=2.9.9